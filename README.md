# DaivikPooja – Sanatan Seva Platform

Full-stack puja booking platform: customer site, pandit partner portal and admin panel, backed by a REST API and a SQLite database.

- **Backend:** Node.js, Express, SQLite (better-sqlite3), JWT auth, bcrypt, multer uploads, helmet, rate limiting
- **Frontend:** vanilla JavaScript single-page app in `public/` (no build step). It renders from `GET /api/state` and sends every change to the API
- **Shared rules:** `shared/pricing.js` runs in both the browser (live price preview) and the server (the price that is actually charged)

## Quick start

Requires **Node 22 or newer** (better-sqlite3 13 needs it).

```bash
npm install
cp .env.example .env        # edit values if you like
npm start                   # http://localhost:3000
```

With `DEMO_MODE=true` (default in development) the first start seeds sample pandits, customers and bookings.

| Role | How to log in in demo mode |
|---|---|
| Customer | "Continue with demo account", any of the **10 seeded customers** from the login modal (Aarav Mehta `9876543210`, Priya Nair, Karan Desai, Meera Iyer, Vikram Singh, Sunita Rao, Rahul Sharma, Anjali Patel, Neha Gupta, Arjun Menon), or any 10-digit mobile — OTP `123456` for all |
| Pandit | Partner with us, "Open demo portal", or Pandit login with the 5 demo accounts `9810000001` … `9810000005` and OTP `123456` (10 profiles exist in total; the rest are for the admin KYC demo) |
| Admin | `/#/admin`, `admin@daivikpuja.in` / `AdminPassword@123` (from `.env`) — full access |

Every demo customer has at least one booking; five have saved kundalis generated by the real astrology engine. Demo customers also sign in with email `aarav@example.com` … `arjun@example.com`, password `demo1234`. The admin panel's **Demo data** tab shows live counts plus all demo accounts, generates 1–50 mock bookings through the real booking engine, and can wipe everything back to the fresh demo state (type RESET to confirm; your admin session is renewed automatically). A printable partner hand-out with a 5-minute demo script lives in **PARTNER-DEMO.md**.

## Kundali commercial model (migration 008)

One customer account includes plan-wise free **personal** kundalis (1 / 2 / 5 for customer / Plus / Premium — admin-configurable). **Family-member kundalis are always chargeable**, and additional personal kundalis beyond the quota too. All amounts come from Admin → Kundali settings → pricing (family price, additional price, GST %, discount %, coupon eligibility, free counts, active flag) — nothing hard-coded.

Flow for a paid kundali: customer picks a family member → the form shows the price → server classifies the request (`FREE` within quota, else chargeable) → in Razorpay mode the kundali row is stored as `PENDING_PAYMENT` with an order and the customer only gets the result after `POST /api/kundali/pay/verify` (signature-verified); in mock mode it completes instantly as `PAID`. Repeated submits with the same idempotency key return the original kundali instead of charging twice.

- `GET /api/kundali/pricing` — public pricing + the customer's quota; `POST /api/kundali/quote` — exact quote with GST/coupon
- `GET /api/kundali/mine` — the customer's kundalis with billing states; also shown in Account → My Kundalis with the price panel
- Family members are a real table (`family_members`, CRUD at `/api/me/family*`) with relationship, birth details and coordinates — one source of truth for the booking-wizard member picker and kundali generation
- Ownership: a logged-in customer can only open their own kundalis (404 otherwise); admins see everything in Admin → Kundali settings list (`/api/admin/kundalis`, filterable by kind/billing/search)
- `POST /api/kundali/generate` is rate-limited server-side: 15 requests / 15 min per user-or-IP, answered with a structured 429 before the ephemeris runs

## Reports: Excel exports (26 reports)

Admin → Reports exports 26 reports as real `.xlsx` files (exceljs): customers, customer login/accounts, pandits, pandit login/accounts, temples, pujas, bookings, customized puja requests, kundalis, family members, samagri, prasad, orders, payments, refunds, coupons, campaigns, payouts, revenue by month, commission by month, puja performance, pandit performance, customer activity, login activity, audit logs and puja media. Exports always contain **all records matching the filters** (status/date/mode/billing), reuse the same queries the admin tables show, exclude sensitive fields by design (password hashes, OTPs, tokens, KYC), require the admin role, and every download is written to an audit log (`export_logs`, viewable on the same tab). The professional format includes a report title, generated-on timestamp (IST), the applied filters, a frozen filterable header row, auto column widths, Indian-currency number formats and a totals row. Service visibility toggles on the same tab hide Home/Online/Temple/Customized Puja, Kundali, Pandit, Temple, Prasad, Samagri or Astrology from the customer navigation instantly.

## Account management & puja photos (migration 009)

- **Admin → Logins and accounts** — one tab for customers and pandits: login ID (mobile or email) and method, account status, verification, last login (time + method), usage counts and a DEMO flag. Actions: **View** (hashes never shown), **Reset password** (one-time temporary password shown once; the account must change it at next login), **Force change**, and **Suspend / Disable / Activate** — all written to the append-only audit log (`admin → Audit log` and the audit-logs Excel report).
- **Login hardening** — suspended/disabled accounts are refused at every login door and every API call; 5 consecutive wrong passwords lock an account for 15 minutes; last login and method are stamped on the user row and every attempt (success or failure) is recorded in `login_activity`.
- **Puja photos** — admin uploads on the Pujas tab (Photos button) are approved + published instantly; pandit uploads (Pandit portal → *My service photos*) are tied to their own assigned bookings and stay **pending until the admin approves and publishes** them. Only approved + published photos appear on the public puja pages. Uploads are validated by magic bytes (not just the extension), downloads go through `/api/media/:id/download` by database id (path-traversal-proof), and all management is audited. On first boot the platform seeds one freely-licensed photo per puja into the catalogue (see `shared/seed-photos/CREDITS.md`); pujas that already have media are never touched, so admin deletions stick.

## Kundali: place resolution, dosh analysis, English/हिंदी
The kundali flow (`#/kundali`) is fully database-driven and bilingual:

- **Birth place** — searchable city index storing city, state, country, latitude, longitude and timezone. The form shows a **Selected Place** verification box (e.g. `New Delhi, Delhi, India · 28.6139° N, 77.2090° E · UTC+05:30`) before generating, and the result page repeats the full place details. `POST /api/kundali/generate` and `GET /api/kundali/:id` return a structured `place` object.
- **Hindi engine** — the astrology engine emits additive Devanagari fields (`signHi`, `nameHi`, `nakshatraHi`, `dignityHi`, `tithiHi`, `varaHi`, dasha `lordHi`…): 12 राशि, 27 नक्षत्र, 9 ग्रह, उच्च/नीच/स्वराशि. English fields are unchanged, so old saved kundalis keep working and the UI can switch language without regenerating.
- **Header selector** — **English | हिंदी** in the top bar switches the whole customer experience, including the kundali result (लग्न/राशि/नक्षत्र/पंचांग/ग्रह स्थिति/भाव/दशा), dosh cards (name, meaning, why-identified evidence, remedy), seva recommendations, status badges and the disclaimer. Language is persisted in `localStorage`.
- **Hindi catalog** — dosh names/descriptions/remedies, condition→puja mapping reasons and puja benefits carry Hindi columns (migration 006/007) and are re-applied on every boot, so a demo RESET never removes them.

## Puja management & fulfilment

- **Modes** — home, online (video) and temple pujas; 6-step booking wizard; statuses New → Confirmed → Assigned → Started → Completed/Cancelled with tiered refunds and reschedule rules.
- **Pandit assignment** — auto-assign (expertise, city, rating) or admin manual assignment; pandit accept/reject/start/complete with checklist and media upload; availability and slot-conflict enforcement.
- **Samagri & prasad** — kit CRUD with stock and restock, delivery tracking, prasad dispatch; out-of-stock blocks booking and orders.
- **Pricing** — shared `shared/pricing.js` engine: mode factors, pandit factors, Plus discounts, coupons, reward points, GST; identical in browser preview and server charge.
- **Customised pujas** — public request form (`#/custom-puja`) → admin queue (Pujas tab) with statuses New/Contacted/Quoted/Booked/Closed → **Convert** creates a real (hidden) catalogue puja ready to price and publish.
- **Full admin puja editing** — name, Hindi name, category, deity, benefits (English + हिंदी), duration, price, kit and visibility via the Pujas tab.
- **Communication** — WhatsApp/SMS/email notification service on booking, assignment, samagri, prasad, refunds; customer notifications tab; admin push to all customers.

Other commands: `npm test` (API tests + engine tests), `npm run test:ui` (drives the real UI in jsdom), `npm run reset` (clear and reseed), `npm run backup` (timestamped working backup into `backups/` — source + `node_modules` + WAL-checkpointed databases + uploads, zipped; keeps the newest 5, `BACKUP_KEEP` to change), `docker build -t daivikpooja . && docker run -p 3000:3000 -v dp:/data -e JWT_SECRET=... daivikpooja`.

## Hosting (backend + Netlify frontend)

The backend runs on any Node host and **saves all data** (users, bookings, kundalis, payments) to `data/daivikpooja.db` — verified to survive restarts. `npm run build:netlify` produces a live `dist/` that talks to the hosted API.

Short version: deploy this repo on Render/Railway (`npm install`, `npm start`, set `JWT_SECRET`, `ADMIN_EMAIL`, `ADMIN_PASSWORD`), then drag `dist/` onto Netlify after putting the API URL in `dist/config.js`, and set `CORS_ORIGIN` on the backend to the Netlify URL.

Full step-by-step with all options and environment variables: **[DEPLOY.md](DEPLOY.md)**.

## Live demo on GitHub Pages

**https://amit-2013.github.io/DevPooja/**

GitHub Pages only serves static files, so it cannot run Express or SQLite. The demo is built from a
snapshot instead: `npm run build:pages` starts this very app against a throwaway database, lets the
demo seed run, then captures the JSON that `GET /api/state` returns for each role. Every page you
see is rendering genuine seeded data, and `shared/pricing.js` is still the code that prices a booking.

What works: the whole catalogue, puja and pandit pages, temples, festivals, astrology, the
booking wizard with live pricing and coupons, the guide assistant, the customer, pandit and
admin panels (log in with the usual demo accounts), and the read-only version of the
**Kundali → Dosh → Puja recommendation** flow.

What does not: **nothing is saved.** Booking, payments, cancellations, profile edits and admin
changes need a database, and the demo says so instead of pretending. Clone the repo and `npm start`
to use it for real.

```bash
npm run build:pages     # writes dist/ from a throwaway database
npm run preview:pages   # serves it at http://localhost:4173/DevPooja/
```

The deploy runs from `.github/workflows/pages.yml` on every push to `main`, and weekly because the
seed books pujas relative to today: an old snapshot would slowly fill up with past dates. If the
workflow cannot create the Pages site on its first run, set Settings, Pages, Source to
*GitHub Actions* and re-run it.

Because the demo publishes `dist/demo/admin.json`, it contains the seeded admin view, including the
sample customers, leads and payouts. All of it is generated by the demo seed in a temporary
directory; your own `data/` and `uploads/` are never read.

## Project layout

```
server/
  index.js            app setup, security headers, static hosting, error handler
  db.js               SQLite schema (created automatically)
  seed.js             catalogue seed, admin account, demo data
  migrate.js          numbered SQL migrations (applied automatically at boot, `--status` to inspect)
  migrations/         001_core … 008_commercial_kundali (kundali tables, conditions, Hindi columns, custom requests, family members, kundali billing)
  auth.js             JWT sign/verify, role middleware
  routes/             auth.js  customer.js  pandit.js  admin.js  kundali.js
  services/           bookings.js (all business rules)  payments.js  notify.js
  services/astrology/ ephemeris.js  kundaliEngine.js  doshEngine.js  recommendationEngine.js  rules/
  lib/                state.js (role-scoped state)  serialize.js  validate helpers  upload.js
  data/catalog.json   pujas, kits, prasad, temples, festivals
shared/pricing.js     price, GST, coupon, points and refund-tier rules
public/               index.html, css/app.css, js/*.js (kundali.js = kundali form + result page)
tests/                api.test.js  astro.test.js  ui-smoke.js
tools/                build-pages.js (Pages snapshot)  serve-pages.js (preview)
.github/workflows/    pages.yml (build and deploy the static demo)
```

## What the backend enforces

- **Double-booking is impossible:** a partial unique index on (pandit, date, slot) rejects overlaps, even under concurrent requests
- **Prices are computed on the server** from the database. The browser preview is only a preview
- Coupons, reward points, kit stock and temple availability are validated on the server; stock and points are released when a booking is cancelled or a payment hold expires
- Refund tiers: 100% more than 48 h ahead, 75% from 24 to 48 h, 50% inside 24 h
- Customers only receive their own data. Pandits see their own bookings with customer mobile numbers masked. KYC documents are stored outside the public folder and streamed only to admins
- OTPs are hashed, expire in 5 minutes and lock after 5 wrong attempts. Rate limits on login, OTP and lead forms
- Payouts are created when a puja is completed, using the commission rate at that moment

## API overview

Public: `GET /api/state`, `POST /api/quote`, `POST /api/leads`, `POST /api/auth/otp/send`, `/otp/verify`, `/email`, `/admin`, `POST /api/pandit/register` (multipart)

Kundali (public, rate-limited): `GET /api/kundali/places?q=`, `/conditions`, `/catalog`, `POST /api/kundali/generate`, `GET /api/kundali/:id` — generates a real sidereal chart (Lahiri), runs the configurable dosh rules and returns ranked puja recommendations from the database

Customer (Bearer token): `PATCH /api/me`, `/me/addresses`, `/me/family`, `/me/plus`, `POST /api/bookings`, `/bookings/:id/cancel|reschedule|review`, `POST /api/payments/verify`, `POST /api/orders`, `POST /api/tickets`

Pandit: `POST /api/pandit/bookings/:id/accept|reject|start|complete` (complete accepts photo/video uploads), `/availability`, `PATCH /profile`

Admin: `/api/admin/...` bookings (assign, status, refund, escalate, ops, manual), pandits (KYC, feature, documents), pujas, settings, coupons, payouts, banners, campaigns, push, inventory, orders, tickets, review moderation, `kundali/conditions` (list, edit, add), `kundali/rules` (map a condition to a puja with priority + reason), `kundali/analyses` (recent kundalis with their doshas and recommendations)

## Going live: checklist

1. Set `NODE_ENV=production`, a strong `JWT_SECRET`, and `ADMIN_EMAIL` / `ADMIN_PASSWORD`. `DEMO_MODE` turns off by default (it must be off in production: it enables fixed OTP `123456` and one-click demo logins)
2. Configure an SMS provider (`TWILIO_*`). Without it, mobile OTP codes cannot be delivered
3. Set `PAYMENT_MODE=razorpay` with your keys and test with Razorpay test mode. Add a Razorpay webhook (`payment.captured`, secret in `RAZORPAY_WEBHOOK_SECRET`, URL `/api/webhooks/razorpay`) as a second, idempotent confirmation path in case the customer closes the browser after paying
4. Serve behind HTTPS (nginx, Caddy, or your host) and set `TRUST_PROXY=1`
5. Back up `data/` and `uploads/` (or move to Postgres and object storage before running more than one server)
6. Have the terms, privacy policy, GST invoicing format and refund policy reviewed by your CA and lawyer

## Known gaps (not built or not verifiable here)

- **Razorpay and Twilio/SendGrid were not run against live accounts.** The order creation, signature check, webhook idempotency and hold-expiry logic are covered by tests using a stubbed gateway; use test keys first
- **Live video puja** is a placeholder screen. Integrate Jitsi, Daily or Zoom and store the room link on the booking
- **Standalone samagri/prasad orders** are placed as pay-on-delivery. **DaivikPooja Plus** and **featured listings** activate without billing in mock mode and return 501 in Razorpay mode until you add subscription billing
- **WhatsApp** through Twilio needs a WhatsApp Business sender and approved message templates
- **Customized-puja payment** ends at `PAYMENT_PENDING`/`PAID` bookkeeping on the request; in Razorpay mode wire the quote amount to a real order the same way bookings do (the mock path completes the workflow for demos)
- **Temple payouts and outstation/travel charges** follow the platform commission setting; per-temple agreements and zone-based travel calculators are not built
- **Kundali engine accuracy:** positions come from a self-contained Meeus-based sidereal engine, validated against JPL Horizons to within ~1–4 arc-minutes for 1990–2026 — good for every Jyotish purpose (a nakshatra pada spans 3°15′). It is not a high-precision ephemeris; swap in Swiss Ephemeris via `services/astrology/ephemeris.js` if you ever need arc-second work
- **Dosh rules are traditional, not predictive:** each result explains its evidence and the language avoids guarantees. Keep it that way, and have a jyotishi review the seeded rule text before you go live
- **Analytics** are computed in the browser from the admin state, which is fine for thousands of bookings but should move to SQL aggregates as you grow
- **Data protection:** the app stores names, mobile numbers and addresses. Add consent capture, a data-deletion flow and retention rules to meet the DPDP Act
- **The Pages demo is read-only.** Writes are refused with an explanation rather than simulated, so nobody mistakes a static snapshot for a working database
- SQLite suits a single server. For several servers or high traffic, switch to Postgres
